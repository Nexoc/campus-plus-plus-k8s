# Backend service secrets example
# Add service-specific secret values here if needed in the future.
# Backend currently mainly needs DB credentials via shared DB secret set.

DB_USERNAME=replace-with-real-db-username
DB_PASSWORD=replace-with-real-db-password
