apiVersion: v1
kind: Service
metadata:
  name: kube-state-metrics
  namespace: kube-system
spec:
  type: NodePort
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      protocol: TCP
      nodePort: 30091
