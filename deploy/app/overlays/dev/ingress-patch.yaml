apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: campus
spec:
  rules:
    - host: campus-dev.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: campus-nginx
                port:
                  number: 80