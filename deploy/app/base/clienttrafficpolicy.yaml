apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: campus-edge
  labels:
    app.kubernetes.io/name: campus-edge
    app.kubernetes.io/component: gateway-policy
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: campus
  clientIPDetection:
    xForwardedFor:
      numTrustedHops: 1
