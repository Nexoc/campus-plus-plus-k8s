apiVersion: v1
kind: Service
metadata:
  name: campus-nginx
  labels:
    app.kubernetes.io/name: campus-nginx
    app.kubernetes.io/component: gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: campus-nginx
    app.kubernetes.io/component: gateway
  ports:
    - name: http
      port: 80
      targetPort: http