# Runtime-only file. Do not commit real database credentials.
# Install as: /home/nexoc/campus-secrets/monitoring/postgres-exporter.env

DATA_SOURCE_NAME=postgresql://<USER>:<PASSWORD>@localhost:5432/<DATABASE>?sslmode=disable
