# Runtime-only file. Do not commit real notification credentials.
# Install as: /home/nexoc/campus-secrets/monitoring/alertmanager.env

# Example placeholders for future notification routing.
# Leave unset until Alertmanager routes are implemented.
ALERTMANAGER_SMTP_HOST=
ALERTMANAGER_SMTP_USERNAME=
ALERTMANAGER_SMTP_PASSWORD=

