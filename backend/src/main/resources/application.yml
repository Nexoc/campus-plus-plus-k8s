# =========================
# Common application config
# =========================

spring:
  application:
    name: backend

# =========================
# Logging configuration
# =========================
# NOTE:
# Logging MUST NOT be under spring.logging
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n"